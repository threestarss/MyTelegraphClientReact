import { makeStyles } from "@material-ui/core";

export const useSearchStyles = makeStyles({
  root: {
    display: "flex",
    flexWrap: "wrap",
  },
  loadmore: {
    margin: "0 8px 12px",
  },
});
