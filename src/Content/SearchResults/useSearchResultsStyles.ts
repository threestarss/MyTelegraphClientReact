import { makeStyles } from "@material-ui/core";

export const useSearchResultsStyles = makeStyles({
  root: {
    display: "flex",
    flexWrap: "wrap",
  },
  loadmore: {
    margin: "0 8px 12px",
  },
});
